AR_SECRET_KEY=replace-with-32-byte-base64-key-from-ar-keygen

# Select which provider backs both proposer + judge calls.
# Values: anthropic | openai
AR_LLM_PROVIDER=anthropic

# API keys (use AR_*-prefixed vars; compose passes these through directly).
AR_ANTHROPIC_API_KEY=
AR_OPENAI_API_KEY=
