AR_SECRET_KEY=replace-with-32-byte-base64-key-from-ar-keygen

# One provider is required to run the loop.
# - Proposer defaults to Anthropic (recommended).
# - Judge defaults to OpenAI.
ANTHROPIC_API_KEY=
OPENAI_API_KEY=
